@model NatisTracker.ViewModels.LogInfoViewModel

<!DOCTYPE html>
@{
    ViewBag.Title = "Scan Back To Safe";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<html lang="en">
<head>
    <meta charset="utf-8">
    <title>Scan Back to Safe &middot; FindMy Natis</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="">
    <meta name="author" content="">

    <style type="text/css">
        .container {
            height: 100%;
            padding: 5px 5px 5px 5px;
            margin-left: 1%;
            margin-right: 1%;
            width: 98%;
        }

        table {
            font-family: arial, sans-serif;
            border-collapse: collapse;
            width: 100%;
        }

        td, th {
            border: 1px solid #dddddd;
            text-align: left;
            padding: 8px;
        }

        tr:nth-child(even) {
            background-color: #dddddd;
        }

    </style>

</head>

<body>
    <h3>Please scan Natis</h3>
    <div class="container">
        <div class="tab-content">
            <div id="Menu" class="tab-pane fade in active">

                @using (Html.BeginForm("Scan_Back_To_Safe", "Process", FormMethod.Post, new { enctype = "multipart/form-data" }))
                {
                    if (Model.contractNo != null)
                    {
                        <div class="table-responsive table table-striped">
                            <table class="table">
                                <thead>
                                    <tr>
                                        <th>Contract Number</th>
                                        <th>VIN Number</th>
                                        <th>Date Scanned</th>
                                        <th>User</th>
                                        <th>Natis Location</th>
                                        <th>Contract Status</th>
                                        <th>Comment</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <tr>
                                        <td>@Model.contractNo</td>
                                        <td>@Model.VIN</td>
                                        <td>@Model.Date</td>
                                        <td>@Model.user</td>
                                        <td>@Model.department</td>
                                        <td>@Model.status</td>
                                        <td>@Model.comment</td>
                                    </tr>
                                </tbody>


                            </table>
                        </div>
                    }
                    <br /><br />
                    @Html.TextBoxFor(model => model.file, new { type = "file", id = "File", accept = "image/bmp", capture = "camera" })<br>

                    <br /><br />
                    <div class="form-group">

                        @Html.LabelFor(model => model.comment, "Comment", new { @class = "col-md-2 control-label" })
                        <div class="col-md-10">
                            @Html.TextAreaFor(model => model.comment)
                        </div>
                    </div>

                    <br /><br>
                    <button class="btn btn-large btn-primary" type="submit">Scan</button>

                    <p align="right">

                        @Html.ActionLink("Back to previous page", "AdminView", "Users", null, null)
                    </p>
                }
            </div>
        </div>

    </div>


</body>

</html>